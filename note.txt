#will be developed
3. middleware
6. return options ok in router api
7. set up api router so it work with frontend javascript framework

#already been developed
1. route for web and api but still neeed to be set up for api
2. respon render with ejs
3. respon json as api server
4. static server in folder public
5. call controller in routes like laravel
2.1. for the database it will be select(all, couple of column), insert, update, delete, and inner join
2.2 migration
4. cli commands for generate file

#fail
2. database connection in model without orm #used query builder kysely(library)
5. respon view in 404 #cancel