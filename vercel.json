{
  "routes": [
    { "src": "/api/(.*)", "dest": "/router/api.ts" },
    { "src": "/(.*)", "dest": "/router/web.ts" }
  ],
  "builds": [
    {
      "src": "index.ts",
      "use": "@vercel/bun"
    }
  ]
}
